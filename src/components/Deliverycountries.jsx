import React from "react";

const Deliverycountries = (props) => {
  return (
    <section>
      <button className="countryname-only">{props.countryname}</button>
    </section>
  );
};

export default Deliverycountries;
